<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Entry <%= id %> • Usage Dashboard</title>
  <style>
    :root {
      color-scheme: light;
      font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background-color: #f7f9fc;
      color: #142538;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background: linear-gradient(180deg, #f7f9fc 0%, #eef3fb 100%);
      padding: 2.5rem 3rem 4rem;
    }

    a.back-link {
      color: #3459d9;
      text-decoration: none;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
    }

    h1 {
      margin: 1.5rem 0 0.2rem;
      font-size: 2rem;
    }

    p.timestamp {
      margin: 0;
      color: #4c6276;
    }

    section.card {
      margin-top: 2rem;
      background: rgba(255, 255, 255, 0.92);
      border-radius: 18px;
      box-shadow: 0 16px 45px rgba(26, 52, 84, 0.1);
      padding: 2rem 2.2rem;
      backdrop-filter: blur(10px);
    }

    dl {
      margin: 0;
      display: grid;
      grid-template-columns: minmax(12rem, 16rem) 1fr;
      row-gap: 1.6rem;
      column-gap: 1.4rem;
    }

    dt {
      font-weight: 600;
      color: #244466;
      font-size: 0.95rem;
    }

    dt span.description {
      display: block;
      margin-top: 0.35rem;
      font-weight: 400;
      font-size: 0.8rem;
      color: #5c7084;
    }

    dd {
      margin: 0;
      font-size: 0.95rem;
      color: #1f3244;
      line-height: 1.55;
      word-break: break-word;
    }

    pre {
      margin: 0;
      background: #f0f4fb;
      border-radius: 12px;
      padding: 1rem;
      font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace;
      font-size: 0.8rem;
      color: #203549;
      overflow-x: auto;
      white-space: pre-wrap;
      word-wrap: break-word;
    }

    .placeholder {
      color: #6d8294;
      font-style: italic;
    }
  </style>
</head>
<body>
  <a class="back-link" href="/">← Back to entries</a>
  <h1>Entry <span style="font-size: 1.25rem; font-weight: 400; color: #4b6276;">#<%= id %></span></h1>
  <p class="timestamp">Logged at <%= timestamp %></p>

  <section class="card">
    <dl>
      <% detailFields.forEach((field) => { %>
        <dt>
          <%= field.label %>
          <span class="description"><%= field.description %></span>
        </dt>
        <dd>
          <% if (field.displayValue === '—') { %>
            <span class="placeholder">—</span>
          <% } else if (field.isPreformatted) { %>
            <pre><%= field.displayValue %></pre>
          <% } else { %>
            <%= field.displayValue %>
          <% } %>
        </dd>
      <% }) %>
    </dl>
  </section>
</body>
</html>

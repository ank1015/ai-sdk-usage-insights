<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Usage Dashboard</title>
  <style>
    :root {
      color-scheme: light;
      font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background-color: #f7f9fc;
      color: #1a2a3a;
    }

    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(180deg, #f7f9fc 0%, #f0f4fb 100%);
      min-height: 100vh;
    }

    header {
      padding: 2.5rem 3rem 1rem;
    }

    h1 {
      margin: 0 0 0.5rem;
      font-size: 2rem;
    }

    p.description {
      margin: 0;
      color: #4a6072;
    }

    main {
      padding: 0 3rem 3rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      overflow: hidden;
      border-radius: 16px;
      background-color: #ffffffcc;
      box-shadow: 0 8px 30px rgba(15, 33, 50, 0.08);
      backdrop-filter: blur(8px);
    }

    thead {
      background: #e7eef9;
    }

    th, td {
      padding: 0.85rem 1rem;
      text-align: left;
      border-bottom: 1px solid rgba(17, 42, 71, 0.08);
      vertical-align: top;
    }

    th {
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #526981;
      font-weight: 600;
    }

    tbody tr:last-child td {
      border-bottom: none;
    }

    tbody tr:hover {
      background: rgba(70, 119, 190, 0.08);
    }

    .tag {
      display: inline-block;
      padding: 0.1rem 0.55rem;
      margin-right: 0.4rem;
      margin-bottom: 0.35rem;
      border-radius: 999px;
      background: rgba(63, 126, 207, 0.12);
      color: #2d5f95;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .muted {
      color: #5b6f80;
      font-size: 0.9rem;
    }

    .preview {
      color: #23384a;
      max-width: 22rem;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    a.button {
      background-color: #3d6cff;
      color: #fff;
      text-decoration: none;
      padding: 0.45rem 0.9rem;
      border-radius: 10px;
      font-weight: 600;
      font-size: 0.85rem;
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      transition: background-color 0.2s ease, transform 0.2s ease;
    }

    a.button:hover {
      background-color: #3459d9;
      transform: translateY(-1px);
    }

    .empty-state {
      padding: 2rem 0;
      text-align: center;
      color: #5e7487;
      font-style: italic;
    }
  </style>
</head>
<body>
  <header>
    <h1>Usage Dashboard</h1>
    <p class="description">Every recorded model call at a glance. Select a row for the full context.</p>
  </header>
  <main>
    <% if (!entries.length) { %>
      <div class="empty-state">No entries were found in this database yet.</div>
    <% } else { %>
    <table>
      <thead>
        <tr>
          <th scope="col">Timestamp</th>
          <th scope="col">Model</th>
          <th scope="col">Tags</th>
          <th scope="col">Input</th>
          <th scope="col">Tokens</th>
          <th scope="col">Latency</th>
          <th scope="col">Details</th>
        </tr>
      </thead>
      <tbody>
        <% entries.forEach((entry) => { %>
          <tr>
            <td><span class="muted"><%= entry.formattedTimestamp %></span></td>
            <td><%= entry.modelId || '—' %></td>
            <td>
              <% if (entry.tags.length) { %>
                <% entry.tags.forEach((tag) => { %>
                  <span class="tag"><%= tag %></span>
                <% }) %>
              <% } else { %>
                <span class="muted">—</span>
              <% } %>
            </td>
            <td><div class="preview" title="<%= entry.inputPreview || '' %>"><%= entry.inputPreview || '—' %></div></td>
            <td><span class="muted"><%= entry.totalTokens != null ? entry.totalTokens : '—' %></span></td>
            <td><span class="muted"><%= entry.latencyMs != null ? `${entry.latencyMs} ms` : '—' %></span></td>
            <td><a class="button" href="/entries/<%= entry.id %>">Open</a></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <% } %>
  </main>
</body>
</html>
